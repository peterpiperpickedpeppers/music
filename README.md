# music

Static HTML webapp for GitHub Pages.

## Pages setup

1. In GitHub, go to **Settings → Pages**.
2. Under **Build and deployment**, choose **Deploy from a branch**.
3. Select branch `main` and folder `/ (root)`.

After Pages is enabled, your app will be available at:

- `https://<username>.github.io/music/`
