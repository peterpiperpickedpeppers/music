# music

This site now uses the GitHub Pages Minimal theme via Jekyll configuration:

- `_config.yml` sets `theme: jekyll-theme-minimal`.
- `index.html` uses Jekyll front matter (`layout: default`) so the theme layout/styles are applied.

